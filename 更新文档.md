# Youtube 项目更新文档

更新时间：2026-05-30

这个文档用于长期记录本程序的更新方向、已经完成的功能、待做功能、项目文件结构和每次处理视频时需要保留的资料。以后程序继续更新时，优先把变化追加到这里。

## 1. 项目边界

- 项目工作目录固定为：`N:\C\Youtube`
- 程序文件、下载文件、字幕文件、切片文件都应放在这个目录或它的子目录里。
- 外部来源文件可以读取，但不直接修改原件。需要处理时，先复制一份到本项目目录。
- C 盘只可能出现 Python、Codex、系统缓存或运行环境路径，不作为本项目资料保存位置。

## 2. 当前已经完成

### 字幕烧录

- 修复了双语字幕烧录后背景框和文字不对齐的问题。
- 字幕背景改为由 libass 原生背景盒绘制，减少手动计算字体高度造成的偏移。
- 烧录前会读取视频真实分辨率，让字幕坐标按视频尺寸生成。

### 本地字幕工作台

已增加“本地字幕工作台”，用于处理本地视频、音频或已有 SRT 字幕。

当前流程：

1. 读取本地视频、音频或 SRT。
2. 如果是视频或音频，使用本地 Faster-Whisper 识别。
3. 合并短句。
4. 使用大模型检查英文单词、专有名词、大小写和标点。
5. 应用校正结果。
6. 翻译中文字幕。
7. 导出英文、中文或中英双语字幕。
8. 支持选择英文在上或中文在上。

### 启动方式

- 新增 `打开网页版.bat`，用于直接打开网页版本。
- 新增 `open_web.py`，用于启动本地网页服务并打开浏览器。

### 录音识别测试

已测试文件：

- 原始微信文件：`J:\WWW\xwechat_files\wxid_r2lzhmvyamcs22_dd78\msg\file\2026-05\新录音 3.m4a`
- 项目内保留副本：`N:\C\Youtube\downloads\local_sources\20260530_新录音_3\source.m4a`
- 识别字幕：`N:\C\Youtube\downloads\local_sources\20260530_新录音_3\source.srt`

这次测试确认：原文件不动，先复制到项目目录，再生成字幕文件。

## 3. 目标工作流

目标是把程序逐步更新成一个完整的视频处理流水线：

1. 下载或读取源视频。
2. 保留源文件和原始信息。
3. 识别英文字幕，生成带时间轴的字幕文件。
4. 合并短句。
5. 在线大模型检查英文识别错误。
6. 翻译中文字幕。
7. 导出字幕文件。
8. 生成带字幕的视频成品。
9. AI 根据内容分析切片点。
10. 自动生成 10 个候选切片，每个片段 2-5 分钟。
11. 用户手动调整切片内容。
12. 程序把最终切片选择和处理记录写回项目资料。

## 4. 每个视频项目建议结构

未来建议每个视频单独一个项目文件夹，例如：

```text
downloads\projects\20260530_video_name\
  00_source\
    source.mp4
    source.m4a
    source_meta.json
    source_meta.md

  10_transcription\
    raw_en.srt
    merged_en.srt
    corrected_en.srt
    correction_report.md

  20_translation\
    zh.srt
    bilingual.en-top.srt
    bilingual.zh-top.srt

  30_burned\
    full_with_subtitles.mp4

  40_clips\
    clip_candidates.json
    clip_01.mp4
    clip_01.srt
    clip_01.md

  90_logs\
    process_log.md
    update_notes.md
```

说明：

- `00_source` 保存源文件和原始信息。
- `10_transcription` 保存语音识别、合并、校正相关文件。
- `20_translation` 保存翻译和双语字幕。
- `30_burned` 保存完整带字幕视频。
- `40_clips` 保存切片候选、最终切片和切片字幕。
- `90_logs` 保存本项目每一步处理记录。

## 5. 原始信息需要保存的内容

每个视频项目都应该尽量保存这些信息：

- 原始标题
- 原始简介
- 原始链接
- 来源平台，例如 YouTube、B 站、本地上传
- 频道名称
- 发布时间
- 下载时间
- 原始字幕或平台字幕
- 语音识别结果
- AI 校正结果
- 翻译结果
- 字幕烧录结果
- 切片候选
- 最终选择的切片
- 用户手动调整记录

## 6. 在线识别计划

当前识别方式是本地 Faster-Whisper。后续计划增加在线识别来源：

- 本地 Faster-Whisper：离线可用，但更新速度和模型能力有限。
- 火山引擎 Whisper：英文识别更适合测试，目标是直接生成带时间轴的 SRT。
- 阿里云百炼 / Paraformer / Fun-ASR：适合带时间戳的字幕工作流。

优先级建议：

1. 先接火山英文识别。
2. 识别结果统一转成 SRT。
3. 后续流程仍然走合并短句、AI 检查、翻译、导出。
4. 不让大模型修改时间轴，只让它修改文字。

## 7. 切片计划

切片功能目标：

- 根据完整字幕分析内容结构。
- 自动生成 10 个候选切片。
- 每个候选片段长度控制在 2-5 分钟。
- 切点优先选择话题自然转换处。
- 每个切片生成标题、摘要、关键词、起止时间。
- 用户可以调整切片起止时间和标题。
- 调整后的切片信息保存回项目资料。

候选切片记录格式建议：

```json
[
  {
    "index": 1,
    "title": "片段标题",
    "start": "00:03:10",
    "end": "00:06:45",
    "summary": "这一段讲了什么",
    "keywords": ["关键词1", "关键词2"],
    "selected": false,
    "user_note": ""
  }
]
```

## 8. 更新记录

### 2026-05-30

- 新增本更新文档。
- 新增 `README.md`，用于 GitHub 首页说明。
- 新增 `.gitignore`，默认不上传视频、音频、缓存、日志和本机工具配置。
- 明确项目只在 `N:\C\Youtube` 内保存和更新。
- 明确源文件保留规则：外部文件先复制到项目目录，原件不动。
- 记录本地字幕工作台当前能力。
- 记录未来项目文件夹结构。
- 记录在线识别、字幕成品、切片候选的下一步方向。

## 9. 下一步开发清单

- 增加“新建视频项目”功能，自动创建项目文件夹。
- 下载视频后自动保存源文件和 `source_meta`。
- 本地上传视频后也进入同样的项目文件夹结构。
- 增加火山在线英文识别选项。
- 识别后自动生成 `raw_en.srt`。
- 合并短句后生成 `merged_en.srt`。
- AI 校正后生成 `corrected_en.srt` 和校正报告。
- 翻译后生成中文字幕和双语字幕。
- 增加字幕效果预览。
- 增加完整视频烧录导出。
- 增加 AI 内容分析和 10 个切片候选。
- 增加切片候选手动调整界面。
- 选中切片后导出剪辑文件和对应字幕。
